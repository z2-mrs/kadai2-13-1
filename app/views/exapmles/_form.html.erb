<%= form_for(exapmle) do |f| %>
  <% if exapmle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exapmle.errors.count, "error") %> prohibited this exapmle from being saved:</h2>

      <ul>
      <% exapmle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :message %>
    <%= f.text_field :message %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
